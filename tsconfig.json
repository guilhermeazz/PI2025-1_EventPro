{
  "compilerOptions": {
    "target": "es2016",
    "module": "commonjs",
    "esModuleInterop": true,
    "forceConsistentCasingInFileNames": true,
    "strict": true,
    "skipLibCheck": true,
    "outDir": "./dist",
    "rootDir": "./src", // ✅ CORREÇÃO: A raiz dos seus arquivos TS é 'src'.
    "resolveJsonModule": true,
    "types": ["node", "express"], // Garante que tipos de Node e Express são incluídos
    "sourceMap": true // Gera source maps para depuração (útil no Railway também)
  },
  "include": ["src/**/*.ts"], // Garante que todos os .ts em src sejam compilados
  "exclude": ["node_modules", "dist"],
  // Remova a linha "rootDirs": ["api"] se ela estava causando problemas ou não é estritamente necessária para outra coisa.
  // Se ela estava comentada, mantenha-a comentada.
}